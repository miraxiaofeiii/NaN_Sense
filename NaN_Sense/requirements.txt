# Core
pandas>=2.2
numpy>=1.26
scikit-learn>=1.4
scipy>=1.11
pyarrow>=15

# NLP / DL
# NOTE: Install the correct Torch build for your system (CPU/CUDA) from https://pytorch.org/get-started/locally/
torch
transformers>=4.42
evaluate>=0.4.1
ftfy>=6.1

# Optional utilities
tqdm>=4.66
